# SabiDrive

School bus tracking + child safety app. Full product spec: [SPEC.md](SPEC.md). Build plan and Phase 1/2 scope cut line: see the plan this repo was built from (ask the user if it's not in your context — it lays out what's deliberately deferred).

## Architecture

Two separate Next.js 14 (App Router, TypeScript) web apps sharing one Supabase backend:

- **`apps/family`** — parent + driver (+ stubbed student) roles. Installable PWA (`@ducanh2912/next-pwa`).
- **`apps/admin`** — school admin / transport coordinator. Plain web app, no PWA.
- **`packages/ui`** — shared components (`Button`, `Card`, `StatusPill`, `Banner`, `TripMap`/`FleetMap`/`ClickToAddMap`) and the calm-palette design tokens (`src/tokens/colors.ts`).
- **`packages/supabase`** — browser/server Supabase client factories, a `SupabaseProvider` context, realtime hooks (`useSession`, `useTripLocation`, `useNotifications`, `useFleetTrips`), and typed query/RPC wrappers under `src/queries/` (`trips`, `students`, `admin`, `push`).
- **`packages/gps-sim`** — pure route-interpolation + geofence math (`src/engine.ts`, `src/route-utils.ts`) and the local dev ticker (`src/run-local.ts`) that simulates bus GPS movement.
- **`supabase/migrations`** — schema, RLS policies, and RPCs, applied in numeric order.
- **`scripts/seed.ts`** — creates one demo school, a route+stops, a bus, students, guardian links, and one auth account per role.

Workspace-internal packages (`@sabidrive/ui`, `@sabidrive/supabase`, `@sabidrive/config`) are consumed as TypeScript source directly (no build step) — both apps list them in `next.config.mjs`'s `transpilePackages`.

## Commands

```
pnpm install              # from repo root
pnpm dev                  # both apps via turbo (family :3000, admin :3001)
pnpm --filter family dev  # just the family app
pnpm --filter admin dev   # just the admin app
pnpm sim:dev              # local GPS ticker — needed for any trip to actually move
pnpm db:seed              # seed demo data (needs .env.local at repo root)
pnpm lint / pnpm typecheck / pnpm build
```

Running the app for real requires **three processes**: `pnpm --filter family dev`, `pnpm --filter admin dev`, and `pnpm sim:dev` — a driver's "Start Trip" does nothing visible without the simulator running.

## Supabase project

- Project ref `ubslfmtqebuuxujohksd` (org "Matombo Agency"), URL `https://ubslfmtqebuuxujohksd.supabase.co`. Provisioned via the Supabase MCP tools (`mcp__claude_ai_Supabase__*`), not the CLI.
- `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` go in `.env.local` in **both** `apps/family` and `apps/admin` (public, safe in the browser).
- `SUPABASE_SERVICE_ROLE_KEY` goes only in a root-level `.env.local` (read by `scripts/seed.ts` and `packages/gps-sim/src/run-local.ts`). Never import it into browser code.
- After any migration change, regenerate `packages/supabase/src/types.gen.ts` via the `generate_typescript_types` MCP tool and re-run `get_advisors` as a security/perf gate.

## Deployment (Vercel)

Two separate Vercel projects under the `godfrey5` team, one per app — matches the two-app architecture, not one project with two output dirs:

- **`family`** → https://sabidrive-family.vercel.app (the original `family-six-theta.vercel.app` domain stopped resolving at some point mid-session — same class of issue as `admin`'s deleted domain — and was replaced with this one)
- **`admin`** → https://sabidrive-admin.vercel.app (re-added after the original `admin-nine-tau-50.vercel.app` domain was deleted mid-session; the project itself was never deleted)

Since these are two separate deployments with no shared entry point, `apps/family/src/app/start/page.tsx` (**https://sabidrive-family.vercel.app/start**) is a single unified landing page linking out to both: "Parent or Driver" stays in the family app (`/login`), "School Admin" is a plain external link to the admin app's `/login`. It's additive — the existing root-page auto-redirect logic in both apps (straight to `/login` or a role home, depending on session) is untouched, so this doesn't change any existing sign-in flow, it's just a new front door for someone who doesn't already have either URL bookmarked.

Each project's **Root Directory** is set (via the Vercel API/dashboard, `Settings → General → Root Directory`) to `apps/family` / `apps/admin` respectively — required for Vercel to correctly detect the pnpm workspace and run `pnpm install` at the true monorepo root rather than treating the subfolder as an isolated project (a plain `vercel deploy` run *from inside* `apps/family` uploads only that subfolder and fails with `npm install` errors — confirmed the hard way).

**Both projects are Git-connected to `Papagof/sabidrive`** (production branch `main`) via the Vercel GitHub App — every push to `main` auto-deploys both `family` and `admin` in parallel, each building from its own Root Directory. No CLI step needed for normal deploys.

**Manual CLI redeploy** (fallback — e.g. to redeploy without a new commit): each app has its own `apps/*/​.vercel/project.json` (gitignored) from `vercel link`. Because of the Root-Directory-detection issue above, a manual deploy must happen with the **monorepo root as CWD**, using a `.vercel/project.json` at the repo root copied from the target app's — e.g. to redeploy family:
```
cp apps/family/.vercel/project.json .vercel/project.json
vercel deploy --prod --token=<VERCEL_TOKEN>   # run from the repo root
```
Swap in `apps/admin/.vercel/project.json` to redeploy admin instead.

**Environment variables** (set per-project in Vercel, `Production` scope — mirrors each app's `.env.local`):
- `family`: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_VAPID_PUBLIC_KEY`.
- `admin`: the same two `NEXT_PUBLIC_*` vars, plus `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` (server-only, used by `/api/invite-user`) and `FAMILY_APP_URL=https://sabidrive-family.vercel.app` (so invite emails redirect to the deployed family app, not localhost).

**Manual step required**: add `https://sabidrive-family.vercel.app/set-password` to Supabase's redirect-URL allow-list (Dashboard → Authentication → URL Configuration → Redirect URLs) — same requirement as the localhost one, needed before an invite sent from the deployed admin app will actually redirect correctly.

**`ERR_VM_DYNAMIC_IMPORT_CALLBACK_MISSING` on Vercel (manual step required)**: hit during a build, even though `package.json`'s `packageManager: "pnpm@9.15.0"` pin and `pnpm-lock.yaml`'s `lockfileVersion: '9.0'` are internally consistent (confirmed locally — `corepack pnpm --version` resolves to exactly `9.15.0`). First suspected as Vercel not honoring the `packageManager` pin (fixed via `build.env.ENABLE_EXPERIMENTAL_COREPACK=1` in both `apps/family/vercel.json` and `apps/admin/vercel.json`) — that didn't change the error, which points instead at a real corepack bug on Node 20.x itself (corepack fetches/executes a pinned package-manager version via `vm`-based dynamic import, which breaks on some Node 20 builds regardless of which version is pinned). The actual fix is the Node runtime, not the pnpm pin: root `package.json`'s `engines.node` is now pinned to `"22.x"` (was `">=20"`, which permitted Vercel to keep landing on the broken 20.19.4). **Still needs a manual check**: Vercel's dashboard "Node.js Version" project setting can take precedence over `engines.node` depending on how it's configured — confirm/set it to `22.x` for both `family` and `admin` projects (Settings → General → Node.js Version) if the `engines.node` bump alone doesn't clear the error on the next deploy.

**Custom domain (`sabidrive.com`, registered at Hostinger — manual step required)**: the domain is registered at Hostinger but the app is not hosted there — Hostinger tried to build/run this Next.js monorepo directly and failed, which is expected: it needs a Node.js server for SSR/API routes and a pnpm-workspace-aware build, neither of which Hostinger's own hosting was set up for. The fix is DNS-only, pointing the domain at the existing Vercel deployments, split as `sabidrive.com` (bare) → `/start` only, `family.sabidrive.com` → the family app, `admin.sabidrive.com` → the admin app.
- `apps/family/vercel.json` — a host-based rewrite (`has: [{ type: "host", value: "sabidrive.com" }]`) makes the bare domain's `/` serve `/start`'s content without affecting `family.sabidrive.com`'s own root (which keeps the existing session-based redirect to `/login` or a role home). This can't be verified locally — `vercel.json` rewrites are Vercel's edge routing layer, not something `next dev` processes — so it's confirmed by a build-succeeds check only; the real proof is the next deploy once the domain is attached.
- Two manual steps, neither of which an AI session can do (no Vercel API token in this environment, and Hostinger DNS access requires the account owner):
  1. **Vercel dashboard** (Project → Settings → Domains): add `sabidrive.com` + `family.sabidrive.com` to the `family` project, `admin.sabidrive.com` to the `admin` project.
  2. **Hostinger DNS**: an `A` record for the bare `sabidrive.com` → `76.76.21.21` (Vercel's anycast IP — apex domains can't use CNAME per DNS spec), and `CNAME` records for `family` → `cname.vercel-dns.com` and `admin` → `cname.vercel-dns.com`. Vercel's own domain-add flow shows the exact current values to use — confirm against that rather than trusting this file if Vercel's target ever changes.

## Native packaging (`apps/family-native`)

Capacitor shell around the **family app only** (parent/driver — the admin dashboard stays web-only, it's a desktop tool). It ships no web assets of its own: `capacitor.config.ts`'s `server.url` points the WebView straight at the live deployed site (`https://sabidrive-family.vercel.app`), so every push to `main` that redeploys `family` shows up in the native app instantly, no app-store resubmission needed. `www/` is a required-but-unused placeholder Capacitor needs even in remote-URL mode.

- **App icon/splash**: generated via `npx capacitor-assets generate` from `assets/icon.png`/`assets/splash.png`, which `assets/generate-source-images.mjs` rasterizes from `apps/family/public/icons/icon.svg` (the one true source image — rerun the script + `generate` after changing it).
- **Camera permission** (driver QR scanning) is wired on both platforms: `android/app/src/main/AndroidManifest.xml` (`CAMERA` + optional `android.hardware.camera` feature) and `ios/App/App/Info.plist` (`NSCameraUsageDescription`).
- **Android**: fully buildable and verified on this machine (Java 21 + Android SDK already installed). `cd apps/family-native/android && ./gradlew.bat assembleDebug` → `app/build/outputs/apk/debug/app-debug.apk`. Confirmed end-to-end: installed on an emulator (`adb install`), launched, and it correctly rendered the live login screen.
- **iOS**: scaffolded and configured (`ios/App`, Xcode project + Info.plist) but **not buildable in this environment** — no Mac available, and `xcodebuild`/CocoaPods need one. Building/signing/submitting needs either a real Mac or a cloud CI Mac runner (e.g. GitHub Actions' macOS runners, or a service like Codemagic) — not set up yet.
- **Native push (Android/FCM)**: wired — see the dedicated section below. iOS native push is still the known gap (needs a real Apple Developer account + Mac, neither available here); in-app Supabase Realtime notifications work on iOS regardless.
- `appId` is `com.sabidrive.family` — a placeholder reverse-domain identifier, fine for development, but each store just needs it to be globally unique at actual submission time (doesn't require owning `sabidrive.com`).
- **Release signing**: the Gradle wiring is done and verified (`apps/family-native/android/app/build.gradle`'s `signingConfigs.release`, conditional on `app/keystore.properties` existing — same guard pattern as `google-services.json`) — see `apps/family-native/PLAY_STORE_SUBMISSION.md` for the full checklist. The actual production keystore is a permanent, unrotatable credential, so it's generated by the user themselves (`keytool`, in their own terminal) rather than in an AI session; the wiring itself was verified end-to-end with a disposable throwaway keystore (`gradlew bundleRelease` → confirmed genuinely signed via `jarsigner -verify`, then deleted). Not done: the user hasn't generated their real keystore yet, and neither Play Console nor App Store Connect submission has happened.

## Data model & RLS mental model

Tables and relationships are documented top-to-bottom in `supabase/migrations/0002_core_schema.sql`. The access-control shape (`0003_rls_policies.sql`):

- **Parent**: read-only, scoped to their children via `guardian_student_links`, plus their own `notifications`, plus the name/phone/verification status of the driver currently driving one of their children's buses.
- **Driver**: scoped to their assigned bus/route/stops/roster; can only act on a trip they own while it's `in_progress`. Also has read access to `guardian_student_links`/`profiles` (guardian names) and `pickup_overrides` for students on their route, needed for two-factor pickup authorization at drop-off — added in `0015_driver_pickup_visibility.sql` after a live end-to-end test caught the gap.
- **Admin**: full CRUD scoped to `school_id`, plus update rights on their school's driver profiles (to set `verification_status`) and on their own `schools` row (Settings page).
- **Cross-role writes never happen via raw client inserts.** Anything that fans out to *other* users' rows (notifications, alerts, check-ins, trip lifecycle, announcements) goes through `SECURITY DEFINER` RPCs — `start_trip` / `end_trip` / `check_in` (`0004_functions_rpc.sql`), `create_announcement` (`0010_announcements.sql`) — which re-validate `auth.uid()` internally. The one exception is `packages/gps-sim`, a trusted service-role process that writes `trip_locations`/`trip_stop_etas`/alerts directly.

If you add a new cross-role write path, follow that RPC pattern rather than loosening an RLS policy.

**Grant-lockdown gotcha** (bit us twice, see `0006`/`0007`/`0013`/`0014`/`0017`): revoking `EXECUTE` from `anon`/`authenticated` on a **newly created** function often does nothing, because Postgres/Supabase grants a default `EXECUTE` to the bare `PUBLIC` pseudo-role at creation time, which every role inherits regardless of role-specific revokes. `REVOKE ... FROM PUBLIC` is what actually clears it. The security advisor (`get_advisors`) also caches — after a grant fix, verify with a direct query instead of trusting the advisor's next call:
```sql
select r.rolname, has_function_privilege(r.rolname, 'public.fn_name(arg_types)'::regprocedure, 'EXECUTE')
from (values ('anon'), ('authenticated')) as r(rolname);
```

## Coding conventions

- TypeScript strict, no `any`.
- Data reads/writes go through `packages/supabase/src/queries/*.ts` (typed wrappers) rather than ad-hoc `.from(...)` calls scattered in components, where practical.
- Tailwind: use the calm-palette tokens (`brand`, `calm`, `neutral`, `caution`, `critical`) from `packages/ui/src/tokens/colors.ts` via the shared preset — **`critical`/red is reserved for true SOS/emergency states only**, never routine warnings (use `caution`/amber for those).
- Controls should stay large/high-contrast (`min-h-control` = 2.75rem) per the spec's accessibility guidance for time-sensitive pickup/drop-off moments.
- Auth is client-side only in Phase 1 (no `@supabase/ssr` cookie plumbing) — acceptable for the MVP demo, a documented Phase 2 improvement if SSR-protected routes are needed.

## Simulated hardware (Phase 1 stubs)

Per the fixed build decisions, these are deliberately simulated, not real:

- GPS: `packages/gps-sim` interpolates position along a route polyline instead of reading a real tracker — still available as a dev/demo tool (`pnpm sim:dev`), but no longer the only source of `trip_locations`; see "Live GPS (driver's phone)" below for the real-position path.
- RFID/NFC: replaced by `qrcode.react`-generated student QR codes (admin → Students → Manage) scanned via `@yudiel/react-qr-scanner` in the driver's Scan screen (with a manual token-entry fallback for camera-less environments).
- SMS notifications: simulated via the `sms_outbox` table/trigger (Phase 2) — no real gateway. (Phone *verification* is the one exception — see "Phone verification & phone login" below, which uses real Twilio SMS by explicit product decision.)
- CCTV/background-check integrations: still not built.

## Phase 1 scope

Auth + 3 roles (admin, driver, parent), live simulated tracking, QR check-in/out with parent notifications, parent live dashboard + notification timeline, admin fleet map + route/stop/bus/student management + attendance reconciliation. Single school assumed.

## Phase 2 scope (built)

- **Speed & harsh-braking alerts**: `packages/gps-sim/src/telemetry.ts` layers randomized instantaneous speed on top of the constant average `advanceTrip` uses for position math, and `run-local.ts` raises `speeding`/`harsh_brake` alerts (60s per-trip cooldown) when it crosses a threshold.
- **Driver verification badge**: `profiles.verification_status` (`pending`/`verified`/`rejected`), settable by admin on the Buses page, shown to parents on the driver-contact card.
- **Two-factor pickup authorization**: `pickup_overrides` table for same-day overrides (admin → Students → Manage); the driver's Scan screen, on a drop-off scan, looks up authorized guardians + today's overrides (`studentQueries.getPickupInfo`) and requires an explicit confirm tap before the `alight` check-in RPC fires.
- **Alert/incident triage**: `alerts.assigned_to`/`alerts.notes` columns; admin Alerts page has severity filters, "Assign to me", and resolution notes.
- **Announcements**: `announcements` table + `create_announcement` RPC (admin → Announcements) fans out a `type='announcement'` notification to every parent in the school; family app has a dedicated Announcements feed (not tied to a specific child).
- **SMS-fallback outbox**: `sms_outbox` table, populated by an `AFTER INSERT ON notifications` trigger (`queue_sms_fallback`) for `boarding`/`alighting`/`sos`/`mismatch`/`announcement` types where the recipient has a `profiles.phone` on file. Purely simulated — admin → SMS log just reads the outbox, no real gateway.
- **Data retention**: `purge_old_data()` (deletes `trip_locations` >14 days, read `notifications` >90 days) on a nightly `pg_cron` schedule (`0012_data_retention.sql`).
- **Multi-school hardening**: admin Settings page (school name/timezone/geofence, via the existing `schools_update_admin` policy). Isolation was verified live — a second school+admin created via the service-role client saw zero rows across buses/students/alerts/trips belonging to school 1.
- **Real Web Push**: see the dedicated section below — built and wired end-to-end, but blocked on a manual dashboard step.

## Web Push setup (manual step required)

The push pipeline (`notifications` insert → `dispatch_push_notification` trigger → `pg_net.http_post` → `push-dispatch` edge function → `web-push` → browser) is fully deployed and confirmed firing (check `supabase/functions/push-dispatch` logs or `select * from net._http_response`), but returns 500 until these **Edge Function secrets** are set via the Supabase dashboard (Project Settings → Edge Functions → `push-dispatch` → Secrets) — the Supabase MCP tools have no way to set them:

- `VAPID_PUBLIC_KEY` = `BKRyj0_Lo1kgE5Lt0geo4_X3rl3ldVqk6zixrxLa-F3jkZhjxqJVFW22U0eyz6CQTksSpsU3mw4q8zfZYL-NReA` (also in `apps/family/.env.local` as `NEXT_PUBLIC_VAPID_PUBLIC_KEY` — must match)
- `VAPID_PRIVATE_KEY` = `HxLmgZZX9MOtkrOvsYi6JCPoZ0EkEZ7ViH0RNiBijp4`
- `VAPID_SUBJECT` = `mailto:admin@sabidrive.dev` (or any contact URI)
- `PUSH_DISPATCH_SECRET` = `f0b475bff10d901a6a4de081c4a16ca2df77e8cd11a4d0e997c34b1e66054e3b` (must match the Vault secret named `push_dispatch_secret` set in `0016_push_dispatch_trigger.sql` — if you ever rotate it, update both sides via `select vault.update_secret(...)`)

Once set, subscribe from the family app (Parent or Driver home → "Enable notifications" — note `next-pwa`'s own service worker is dev-disabled, but `public/push-worker.js` is a separate, always-active registration) and trigger any notification (e.g. an admin announcement) to see a real push.

(Status: secrets confirmed working — a disposable-school test inserted a real notification for a fake-but-valid push subscription and confirmed the `pg_net` trigger's call to `push-dispatch` now returns `200 "ok"` instead of 500. That verifies the Vault secret matches `PUSH_DISPATCH_SECRET` and the VAPID keys are valid. Not yet confirmed: an actual push landing on a real subscribed browser, which needs a human clicking "Enable notifications" and receiving it — the one leg no automated test can cover.)

## Phone verification & phone login (real SMS, manual step required)

Unlike the rest of this app's deliberately-simulated hardware (GPS/RFID/SMS notifications — see "Simulated hardware" below), phone verification sends a **real** SMS one-time code via Twilio, by explicit product decision. Phone login is still password-based — the phone number is only ever used server-side to look up which account's email to authenticate; there's no passwordless/OTP sign-in.

- `supabase/migrations/0021_phone_verification.sql` — adds `profiles.phone_verified` and a partial unique index on `profiles.phone` (so phone→account lookup at login is never ambiguous and one account can't claim a number another account already verified). A `before update on profiles` trigger (`protect_phone_verified`) forces `phone`/`phone_verified` back to their old values for any writer that isn't `service_role` — without it, `profiles_update_own` (`0003_rls_policies.sql`) would let a client just set `phone_verified = true` directly and skip the OTP entirely. (0021 also created a custom `phone_otp_codes` table for a self-rolled hashed-code approach; `0022_drop_phone_otp_codes.sql` dropped it after switching to Twilio Verify, which owns code state itself — see below.)
- `apps/family/src/app/api/phone/send-otp` and `.../verify-otp` — the family app's **first server-side code paths** (same bearer-token-verification pattern as the admin app's `invite-user`). Both call **Twilio Verify** (`verify.twilio.com/v2/Services/{TWILIO_VERIFY_SERVICE_SID}/...`), not the plain Messages API — Twilio Verify uses its own pre-approved message format, so unlike a custom SMS body it works on a Twilio **trial** account (trial accounts otherwise reject arbitrary message content with error 572006, and can only text numbers added as Verified Caller IDs, which defeats a real per-user verification flow). `send-otp` stages `profiles.phone` (unverified) and calls `POST .../Verifications`; `verify-otp` calls `POST .../VerificationCheck` and flips `phone_verified` only when Twilio reports `status: "approved"`.
- `apps/family/src/app/api/login-with-phone` — public (no bearer token, caller has no session yet). Looks up `profiles` by `phone` where `phone_verified = true` to get the account's email, then calls `signInWithPassword` server-side with an anon-key client. Never returns the resolved email to the client and gives the identical generic "Invalid phone number or password" message whether the phone doesn't exist or the password is wrong, so it can't be used to enumerate which phone numbers have accounts.
- `apps/family/src/app/account/page.tsx` — shared page for both parent and driver roles (linked as "Account" from both home screens) showing email-verified status (read from `auth.users.email_confirmed_at` — already true for every parent/driver account by construction, since accepting the admin's invite link *is* what confirms email) and the phone verify/change flow.

**Manual step required**: needs a real Twilio account — Account SID, Auth Token, and a **Verify Service** (Console → Verify → Services, or created once via `POST https://verify.twilio.com/v2/Services`; unlike the Messages API this needs no phone number of its own). Set as server-only env vars in `apps/family/.env.local` (and later Vercel's `family` project, Production scope): `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_VERIFY_SERVICE_SID` — plus `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`, which the family app now needs for the first time (same names/values as `apps/admin/.env.local`). Works fine on a Twilio trial account (draws from the free trial credit, no upgrade or verified-caller-ID step needed) — confirmed live, code sent to and verified from a real phone.

## Removing staff and guardians

Admin → Staff & Guardians has "Deactivate"/"Remove" controls, but neither is a hard delete of an auth account — both were scoped down from a literal delete after finding real data-integrity/blast-radius problems with one:

- **Drivers**: `trips.driver_id` is a required FK with no `on delete` clause, so a driver who has ever driven a trip can't be hard-deleted without either failing outright or (if the FK were loosened) silently losing the "who drove this" audit trail on every past trip — unacceptable for a child-safety app. Instead, "Deactivate" is reversible: `apps/admin/src/app/api/deactivate-driver/route.ts` (service role, same caller-verification pattern as `invite-user`) flips `profiles.deactivated_at` (`0028_driver_deactivation.sql`, purely an admin-UI marker), unassigns them from any bus they currently drive, and — the part that actually matters — calls `auth.admin.updateUserById(driverId, { ban_duration })` to really block sign-in via Supabase Auth's own ban mechanism, not just a client-side UI check. "Reactivate" undoes all three. Scoped so an admin can only touch a driver in their own school (checked server-side against the caller's own profile, not trusted from the request body — same rule as every other privileged admin route). Known nuance: banning blocks future sign-ins/token refreshes but doesn't force-expire an already-issued access token; a just-deactivated driver's current session lives out its natural expiry.
- **Guardians**: a parent can be linked to children at more than one school (the cross-school guardian feature, `0024`), so "delete this parent" as a literal account deletion would also cut off their access to *other* schools' children — surprising and out of scope for a single school's admin. "Remove from this school" instead deletes only this school's `guardian_student_links` rows for that guardian (`adminQueries.removeGuardianFromSchool`) — a plain client-side delete needs no new RPC, since `gsl_admin_crud` (`0003_rls_policies.sql`) already only lets an admin delete links whose *student* is in their own school; links to students at other schools simply don't match the policy and are silently left alone rather than erroring. The guardian's account and any other school's access are untouched.
- Verified live: non-admin and cross-school callers rejected by `deactivate-driver`; a deactivated driver's password sign-in actually fails and a reactivated one succeeds again; their bus's `driver_id` clears on deactivation; and a guardian linked to students at two different (synthetic, disposable) schools keeps the second school's link after the first school's admin removes them.

## Deleting a bus

Admin → Buses has a real hard-delete button (unlike the driver/guardian removals above) — but only because `trips.bus_id` was changed from `on delete cascade` to the default no-action/RESTRICT (`0029_bus_deletion.sql`, matching `trips.driver_id`'s existing behavior). Before that fix, deleting a bus with any trip history would have silently cascaded away every trip, check-in event, attendance record, and GPS ping tied to it — the DB would have just done it, no error, unlike the driver case which at least fails loudly. Now the database itself refuses the delete for any bus that's ever run a trip (a real FK violation, `error.code === '23503'`), and `adminQueries.deleteBus` throws a typed `BusHasTripHistoryError` for that specific code so the UI can react to it (rather than string-matching an error message) instead of showing a raw Postgres error. A bus that was created and never actually used deletes cleanly.

For a bus that *is* blocked, `0030_bus_retirement.sql` adds the same reversible pattern as driver deactivation: `retired_at` on `buses`, set via `adminQueries.setBusRetired`, which also clears the bus's `driver_id`/`attendant_id`/`default_route_id` — both so it stops occupying a driver/route slot and, as a side effect, so `start_trip`'s existing `driver_id = auth.uid()` check naturally blocks anyone from starting a trip on a retired bus without needing a separate "is this bus retired" check in the RPC. The Buses page surfaces this contextually: attempting to delete a bus with trip history shows a "Retire instead" button right in the failed-delete state, and every non-retired bus also has a standalone "Retire" action; a retired bus shows a "Retired" pill and only a "Restore" button. Verified live: delete is genuinely blocked for a bus with trip history; retiring it clears the assignment and leaves trip history untouched; the (now-unassigned) driver can no longer start a trip on it; and restoring clears `retired_at` again.

**Routes get the same protection for free.** `trips.route_id` already had no `on delete` clause (RESTRICT by default) from the original schema — nobody had to fix it the way `trips.bus_id` needed 0029. `adminQueries.deleteRoute` mirrors `deleteBus` exactly (catches `23503`, throws a typed `RouteHasTripHistoryError`), and Admin → Routes has the same Delete-with-confirm pattern. A route's own `stops` cascade away with it (they're meaningless without their route), while any bus/student still pointing at it via `default_route_id` just gets that pointer cleared (`on delete set null`) rather than blocking — those are assignment pointers, not history. No "retire" equivalent exists for routes (not asked for); a route with trip history simply can't be deleted. Verified live: an unused route deletes and its stop cascades away, a student's `default_route_id` clears rather than blocking; a route with a completed trip is rejected, and both the route and its trip history remain untouched.

## Live GPS (driver's phone)

The first real (non-simulated) GPS source: while a trip is `in_progress`, the driver's own trip page (`apps/family/src/app/driver/trip/[tripId]/page.tsx`) reads their phone's real position via the browser's Geolocation API and reports it to the same `trip_locations` table `packages/gps-sim` writes to — so every consumer (`useTripLocation`, ETAs, the admin fleet map) needed zero changes. `packages/gps-sim` itself is untouched and still available as a dev/demo tool (`pnpm sim:dev`) for a trip nobody is physically driving.

- `apps/family/src/lib/useLiveLocationSharing.ts` — a hook that loads the trip's route geometry once (`tripQueries.getTripRouteGeometry`), then `navigator.geolocation.watchPosition`s, throttled to one report per ~4s. Route-projection math (distance-along-route, deviation-from-route, per-stop ETA) runs **client-side**, reusing `@sabidrive/gps-sim`'s existing pure engine (`computeEtaMinutes`, `projectPointOntoRoute`, `distanceToRouteM`, `cumulativeDistancesM`) instead of re-deriving the same geometry in SQL — this required adding `apps/family` as a consumer of `@sabidrive/gps-sim` (new dependency + `transpilePackages` entry), which previously only `packages/gps-sim`'s own local ticker imported.
- `supabase/migrations/0027_live_driver_gps.sql` — `record_trip_location()` RPC (driver-authenticated, same `driver_id = auth.uid()` + `status = 'in_progress'` pattern as `check_in`/`end_trip`). It trusts the client for the *geometry numbers* (low-stakes — a driver can't meaningfully lie about their own trip's ETA display), but independently re-decides whether those numbers cross an alert/notification threshold, using Postgres-persisted state instead of the in-memory `Map`s `run-local.ts` uses (there's no long-running process here to hold that state):
  - **Speeding / harsh-brake**: re-applies `packages/gps-sim/src/telemetry.ts`'s exact thresholds (1.6x average speed; a 15kmh drop between consecutive fixes, read back from the previous `trip_locations` row) with a 60s cooldown enforced by checking `alerts`' own recent history — no separate state table needed.
  - **Route deviation**: only raises a new alert if no *unresolved* `route_deviation` alert already exists for the trip — simpler than `run-local.ts`'s live in-memory "currently deviated" boolean, at the cost of needing an admin to resolve one (Alerts page) before a later deviation raises another.
  - **Stop-approach notifications**: exactly-once per stop per trip via the new `trip_stop_approaches` table (same no-RLS-policies, server-only-via-RPC pattern as `pickup_codes` from `0026`) — an `insert ... on conflict do nothing` followed by checking `FOUND` is what makes this atomic and idempotent under concurrent calls.
- **Known gap, honestly scoped out**: true harsh-braking really wants an accelerometer, not GPS-derived speed deltas — what's here is a reasonable approximation (consistent with what the simulator was already faking), not real IMU-based detection.
- The driver's trip page shows a status pill ("Sharing live location" / permission-denied / unsupported) — denial is **non-blocking**: QR scanning and ending the trip work regardless of whether location sharing is on.
- Verified live end-to-end (disposable school, real RPC calls over REST with real driver JWTs): unauthorized-driver rejection, normal fix → `trip_locations`/`trip_stop_etas` writes, stop-approach → exactly-one geofence notification (repeat approach does not duplicate it), speeding/harsh-brake/route-deviation alert creation *and* their cooldown/dedup gates, and rejection once the trip is no longer `in_progress`.

## When the driver's phone goes dark

In production there's no fallback simulator running — `packages/gps-sim` is a local dev tool only — so before this, a driver's phone dying, breaking, or getting lost mid-trip meant tracking just stopped, silently, with nothing to fall back on. Three deliberately-separate mitigations, none of which fabricate a fake "live" position (an earlier option — estimating position from route + elapsed time as a fallback — was considered and explicitly rejected: showing a guess as if it were real GPS is a bad tradeoff for a safety app, especially at the exact moment something may be wrong):

- **A second phone as genuine backup GPS** (`0031_attendant_backup_crew.sql`): `buses.attendant_id` already existed in the schema and `start_trip()` already accepted an attendant, but nothing else did — an attendant couldn't check students in/out, end the trip, report location, or even `SELECT` the trip row (`can_view_trip()` only checked `driver_id`). Added `is_trip_crew(p_trip_id)` (`driver_id = auth.uid() or bus.attendant_id = auth.uid()`) and swapped it into `can_view_trip`, `check_in`, `end_trip`, and `record_trip_location`'s authorization checks, so an attendant is now a genuine co-driver who can fully substitute if the primary driver's device fails — not just a location-only role. Admin → Buses gained an "Assign backup driver (attendant)" select (create + edit), reusing the same driver list. No changes were needed to `/driver`'s navigation: `getDriverBus`'s `.or(driver_id.eq,attendant_id.eq)` and the trip page's "Resume trip" button already meant a second device signing in as the attendant lands on the exact same in-progress trip automatically.
- **Honest staleness, not a frozen "live" map** (`packages/supabase/src/hooks/useStaleness.ts`): re-evaluates on a 10s timer (not just when new data arrives, since the whole point is noticing when data *stops* arriving) whether the last `trip_locations.recorded_at` is older than 90s, exported for both the parent's per-child trip page and the admin trip page to show "Location last updated N min ago" instead of letting a stale marker look falsely current.
- **Manual admin override as the true last resort** (`0032_manual_trip_location.sql`): `trip_locations` gained a `source` column (`'gps' | 'manual'`, default `'gps'`) and `record_manual_trip_location(p_trip_id, p_lat, p_lng)`, admin-only (checked against the caller's own school, not request-supplied) and only while the trip is `in_progress` — no alert/ETA logic runs for a manual point, since it isn't telemetry. Admin's trip page (`apps/admin/src/app/attendance/[tripId]/page.tsx`, now also a live-map view, not just the attendance list) surfaces this as a collapsible "Manually update position" panel reusing `AddressSearch` + `ClickToAddMap` verbatim from the route/stop builder, and shows a distinct "set manually, not GPS" banner whenever the latest point has `source = 'manual'`.
- Verified live: an attendant can view/check-in/report-location/end a trip an unrelated driver is rejected from on every one of those; a driver (non-admin) and an admin from a different school both get rejected by the manual-override RPC while the owning admin succeeds and the row lands tagged `source = 'manual'`.

## Home pickup/drop-off SMS codes

An alternative to QR scanning for the two "home end" check-ins — a student boarding at home on a pickup-direction trip, or alighting at home on a dropoff-direction trip. A parent requests a one-time SMS code (reusing the same Twilio Verify service as phone verification, above) from the child's page, and the driver types that code into the Scan screen instead of scanning the QR.

- `supabase/migrations/0026_pickup_sms_codes.sql` — adds `pickup_codes` (student_id, guardian_id, event_type, phone, status, expires_at) with RLS enabled and **no policies at all** — like `phone_verified`, it's only ever touched by service-role Route Handlers that re-validate authorization in code, so a bare client gets zero rows regardless of the table-level grants Supabase auto-applies to `anon`/`authenticated`. Also adds `'sms_code'` to `check_in_events.method`'s check constraint, and gives `check_in()` an optional trailing `p_method` param so a code-based check-in is recorded honestly instead of every row claiming `'qr'`.
  - **Overload trap hit live**: appending a defaulted trailing parameter to `check_in()` via `create or replace function` did **not** replace the original 3-arg function — Postgres treated the 4-arg version as a distinct overload, since exact argument-list match (not just satisfiability via defaults) is what makes `CREATE OR REPLACE` replace-in-place. With both signatures present, every existing 3-arg call became ambiguous (`function is not unique`) because the 4-arg overload's `p_method` default also satisfies a 3-arg call. Fixed by explicitly `drop function`-ing the old 3-arg signature. The new 4-arg function also started with a fresh **PUBLIC** default EXECUTE grant (this project's documented grant-lockdown gotcha, same as `0006`/`0007`/`0013`/`0014`/`0017`) rather than inheriting the original's `authenticated`-only grant — re-locked down explicitly and reverified via `has_function_privilege()`.
  - `purge_old_data()` (`0012_data_retention.sql`) extended to also drop `pickup_codes` rows older than 1 day — they're 10-minute-lived by design.
- `apps/family/src/app/api/pickup-code/request` — guardian-only (verified via `guardian_student_links`, service role), requires the caller's own `profiles.phone_verified`. A new request marks any other pending row for that phone+event as `expired` first, since a fresh Twilio Verification for the same phone number effectively supersedes whatever was pending before — without this, `/verify` could match a stale row Twilio itself no longer honors.
- `apps/family/src/app/api/pickup-code/verify` — called by the driver's Scan screen with a bare 6-digit code plus `tripId`/`eventType`; the code alone doesn't say which student it's for, so this narrows candidates to `pickup_codes` rows for students on *that trip's* roster (`attendance_expectations`) with a matching pending, unexpired row, then calls Twilio's `VerificationCheck` against each candidate's phone until one approves (roster sizes are small — a single bus manifest — so this stays cheap). On a match it calls `check_in(..., p_method: 'sms_code')` using a Supabase client scoped to the driver's own bearer token (`createUserScopedServerSupabaseClient`, `packages/supabase/src/server.ts`) so `auth.uid()` inside the RPC resolves to the real driver, then marks the `pickup_codes` row `consumed`.
- `apps/family/src/app/parent/[studentId]/page.tsx` — "Text pickup code" / "Text drop-off code" buttons (gated on `profile.phone_verified`, linking to Account if not); `apps/family/src/app/driver/trip/[tripId]/scan/page.tsx` — a code-entry field alongside the existing camera/manual-token options, scoped to whichever `eventType` (Boarding/Drop-off) toggle is currently selected.
- Verified live: full auth-guard coverage (missing token, non-guardian, unverified phone, wrong driver, unknown/not-in-progress trip, no matching candidate, wrong code against a real pending candidate) plus the `check_in(p_method='sms_code')` → `check_in_events`/`attendance_expectations`/notification-fan-out pipeline, all via a disposable synthetic school hitting the real Route Handlers over `pnpm --filter family dev`. The one leg not exercised by an automated test is an actual Twilio-approved code (needs a real phone reading a real SMS, same constraint phone verification/login-by-phone had) — that code path is otherwise identical to the already-live-verified `verify-otp` route.

## Admin-invited accounts (Phase 3)

The only way to create a driver/parent account is `scripts/seed.ts` (demo data) **or** an admin inviting them by email from the admin app (Buses/Students "+ Invite new…", or the dedicated Staff & Guardians page) — see below for how a school gets its *first* admin, which is the one account type this doesn't cover.

- `apps/admin/src/app/api/invite-user/route.ts` — the admin app's **first server-side code path**. Verifies the caller's bearer token, confirms `role === 'admin'` via their own `profiles` row (service-role client), then calls `auth.admin.inviteUserByEmail` with `role`/`school_id` taken **only** from the verified caller's profile — never from the request body, so this endpoint can't be used to invite a fake admin or into another school. Needs `SUPABASE_SERVICE_ROLE_KEY` + `SUPABASE_URL` + `FAMILY_APP_URL` in `apps/admin/.env.local` (server-only — the pattern is the same as `scripts/seed.ts`, not a shift to SSR page rendering).
- `apps/family/src/app/set-password/page.tsx` — where the invite email's link lands; supabase-js auto-detects the session from the URL, then the user sets a password and gets redirected by role.
- `profiles.email` (added in `0019_profiles_email.sql`) is a denormalized copy of `auth.users.email`, kept in sync by `handle_new_user` — added because PostgREST can't expose the `auth` schema directly and the Staff page/dropdowns need it for display.

**Manual step required**: Supabase Auth requires `redirectTo` URLs to be on an allow-list (Dashboard → Authentication → URL Configuration → Redirect URLs) — no MCP tool manages this. Add the family app's `/set-password` URL (e.g. `http://localhost:3002/set-password` in dev) before testing the real email → click-link → set-password flow. Also: no custom SMTP is configured, so invites go through Supabase's default email service, which is rate-limited (hit this during verification) — fine for occasional testing, not for real invite volume.

## Self-service school signup

`apps/admin/src/app/signup/page.tsx` (linked from `/login`) is genuinely open/public — any new school creates its own account, no approval step. This is the one account-creation path that predates having any admin: `apps/admin/src/app/api/signup-school/route.ts` takes no bearer token, creates a new `schools` row and its first admin (`auth.admin.createUser`, direct password — not an email invite, avoids the same email rate limit noted above) in one call, with compensating cleanup (deletes the school row) if user creation fails so a rejected signup never leaves an orphan. Same safety rule as `invite-user`: role is hardcoded to `'admin'` and the school is always freshly created server-side, never client-supplied — verified live (a real signup, confirmed the new admin sees zero rows from any other school, and confirmed a duplicate-email attempt leaves no orphaned school behind).

**Address + device location are required at signup** (`0020_schools_address.sql` adds `schools.address`; `geofence_lat`/`geofence_lng` already existed). No paid geocoding API is wired in, so the coordinates come straight from the browser's own Geolocation API (`apps/admin/src/lib/geolocation.ts`) rather than being derived from the typed address — the signup page auto-prompts for location on mount, with a retry button, and the submit button stays disabled until both the address field is filled and a position is captured; `signup-school` re-validates both server-side (rejects a missing address or out-of-range/non-finite lat/lng) so the requirement holds even if the client check is bypassed. The admin dashboard's fleet map now centers on the school's own `address`/`geofence_lat`/`geofence_lng` (shown as a caption) instead of a hardcoded global default, falling back to that default only for schools that signed up before this shipped; Settings has a non-required "Use my current location" button using the same helper so admins can update it later.

## Address search for routes and stops

`packages/ui/src/components/AddressSearch.tsx` lets an admin type an address instead of eyeballing a spot on the map when building a route (`routes/new`) or adding a stop (`routes/[routeId]`) — picking a result both drops the point (same as a map click would) and pans the map there (`ClickToAddMap`'s new `panTo` prop, reactive unlike its mount-only `center` prop). This is forward geocoding (address → coordinates), the opposite direction from school signup's device-location capture above, and a different data source: it calls OpenStreetMap's free **Nominatim** search API directly from the browser, matching the existing precedent of pulling map tiles straight from `tile.openstreetmap.org` client-side rather than going through a paid geocoder or a server-side proxy — reasonable at this app's current scale (an admin tool, not a bulk geocoder), one explicit request per search submit rather than as-you-type, in line with Nominatim's usage policy against automated/heavy querying.

## Multi-school & cross-role guardians

A parent whose children attend two different schools can use **one account** for both — but this isn't "one email registered twice." Supabase Auth requires email to be globally unique per project, so instead a second school's admin attaches the parent's *existing* account as a guardian of their student, rather than inviting a duplicate.

- The parent-facing read path (trips/buses/routes/stops/students/notifications) was already multi-school-safe before this shipped — everything is scoped through `guardian_student_links` → `students.school_id`, never through the guardian's own `profiles.school_id` (which just means "whichever school first invited them," now effectively vestigial for parents). `gsl_admin_crud` (`0003_rls_policies.sql`) only checks the *student's* school too, so an admin linking an existing cross-school guardian to a local student needed zero RLS changes.
- Two things did assume one school per parent, fixed in `0023_multi_school_parents.sql`: `create_announcement()`'s notification fan-out selected recipients by `profiles.school_id` (so a second school's announcements would never reach a parent whose `school_id` pointed elsewhere) — rewritten to join through `guardian_student_links`/`students` instead; and there was no RLS policy letting a parent read a `schools` row outside their own `school_id`, so even though they could already see a second school's students/buses, they couldn't look up its *name* for display — added `schools_select_parent_of_child`.
- `apps/admin/src/app/api/find-guardian-by-email/route.ts` — looks up an existing account by email regardless of role or school (service-role, since an admin's own RLS can't see cross-school profiles), returning only `{found, id, full_name}` — never phone, other children, or which other schools/roles they belong to. Wired into the Students page (`apps/admin/src/app/students/page.tsx`) as a "link a guardian who already has an account at another school" input next to the existing same-school dropdown and email-invite flow. Linking is immediate, no parent-side confirmation step — matching the trust this app already gives admins for same-school guardian linking.
- `apps/family/src/app/parent/page.tsx` groups a parent's children by school (via `getGuardianStudents`' now-embedded `schools(name)`) whenever they span more than one, so it's obvious which bus/route each child belongs to.

**Guardian status is independent of an account's primary role** (`0024_cross_role_guardians.sql`) — an admin or driver can also be a guardian of a child at another school with the *same login*, not just a parent-role account. `students_select_parent` always worked this way (`is_guardian_of(id)`, no role check); `can_view_trip()`, `routes_select_parent`, `stops_select_parent`, `buses_select_parent`, and `profiles_select_guardian_of_route_student` (`0015_driver_pickup_visibility.sql`) previously also required `current_role() = 'parent'` (or the target row's `role = 'parent'`) alongside the guardian check — dropped, since the guardian-link check is already the real authorization boundary and the role clause only served to incidentally block a legitimate cross-role guardian. On the family app, `useRequireGuardianAccess()` (`apps/family/src/lib/useRequireRole.ts`, used by `/parent`, `/parent/[studentId]`, `/parent/announcements`, `/account`) replaced the old `useRequireRole(["parent"])` gate, which would otherwise redirect a non-`'parent'`-role account back to `/parent` in an infinite loop — it now just requires a session, since RLS scopes the data and the pages' existing "no children linked" empty state handles an account with none. `/driver` is untouched and stays strictly role-gated — driving is an operational capability, not a guardian one. A guardian linked this way gets only read-only parent-level visibility into that one child; nothing about their own admin/driver privileges changes.

## Forgot password & hide-password toggle

Both apps' login pages (`apps/family/src/app/login/page.tsx`, `apps/admin/src/app/login/page.tsx`) gained a "Forgot password?" link (email-method only on the family app, since phone login has no associated email to reset against) that swaps to a small inline `view: "forgot"` form calling `supabase.auth.resetPasswordForEmail(email, { redirectTo })`. The success state is a generic "If an account exists for {email}, a password reset link is on its way" banner regardless of whether the email actually matched an account — same anti-enumeration shape as `login-with-phone`'s deliberately generic error message.

- **The reset link lands on the same `apps/family/src/app/set-password/page.tsx` an admin's email invite already uses** — no new page, no new Supabase redirect allow-list entry needed, because supabase-js's `detectSessionInUrl` (on by default) handles an invite-acceptance link and a password-reset link identically: both are magic links that establish a session via URL fragment, and from that page's point of view "user has a session, let them set a password" is the same operation either way. The invalid-link error copy was generalized from invite-only wording to cover both cases.
- The admin app has no `set-password` page of its own, so its reset redirect points cross-app at the family app's, via a hardcoded `FAMILY_APP_URL` constant (same tradeoff already accepted for `/start`'s cross-app link — correct in production, only wrong if testing the admin app locally against a deployed family app).
- **Bug fix caught in passing**: `set-password`'s post-submit redirect only ever sent the user to `/driver` or `/parent` within the family app, so an admin invite's set-password flow was landing an admin account on the parent home screen. Fixed to redirect admin-role accounts to the admin app's `/dashboard` instead (`ADMIN_APP_URL`, same hardcoded cross-app pattern). Found while touching this file for the reset-password generalization, not user-reported.
- `packages/ui/src/components/PasswordInput.tsx` — a shared `forwardRef` wrapper around a native `<input>` adding a show/hide eye-icon toggle button, used everywhere a password is entered or set (both login pages, both `set-password` fields) in place of a raw `type="password"` input.
- Verified live against the real `/auth/v1/recover` endpoint: the family app's allow-listed `set-password` redirect is accepted (200). Also observed, as a non-finding rather than a gap to fix: a non-allowlisted redirect URL is *not* rejected at this specific API layer either — Supabase's redirect enforcement happens elsewhere in the flow, not as a 400 from `/recover` itself.

## Driver SOS / panic button

Both `alerts.type` and `notifications.type` (and the SMS-fallback trigger's fan-out list, `0011_push_and_sms.sql`) have allowed a `'sos'` value since the very first schema, and the whole design system had a matching reserved-but-unused slot — `Button`'s `variant="sos"` (red, `packages/ui/src/components/Button.tsx`), `Banner`'s `tone="critical"` (`role="alert"`), and `statusToneMap`'s `sos: "critical"` entry all predate this feature. None of it was ever wired to an actual trigger until now — a driver had no way to signal an active emergency.

- `supabase/migrations/0033_driver_sos.sql` — `trigger_sos(p_trip_id)`, mirroring `end_trip`/`check_in`'s shape exactly: `is_trip_crew()` authorization (driver or attendant), only while the trip is `in_progress`. Inserts one `alerts` row (`severity = 'critical'`, `payload.triggered_by = auth.uid()`) for the admin's Alerts page, plus a `notifications` row for every admin at the school and one per guardian on the trip's roster (`attendance_expectations` → `guardian_student_links`, same join `check_in`'s boarding/alighting notifications use) — which the existing `queue_sms_fallback` trigger picks up automatically since `'sos'` was already in its fan-out list.
- `apps/family/src/app/driver/trip/[tripId]/page.tsx` — a prominent `variant="sos"` button, gated behind an explicit confirm step (`Banner tone="critical"` with Confirm/Cancel) before it actually fires, then a "sent" confirmation banner with a "Send another alert" option in case the situation continues.
- `packages/supabase/src/queries/trips.ts` — `triggerSos()`; `types.gen.ts` updated by hand (the `generate_typescript_types` MCP tool isn't available in this environment) since `.rpc()` calls are strictly typed against it.
- Verified live end-to-end (disposable synthetic school, real driver/guardian/admin JWTs): an unrelated driver and a guardian are both rejected by `trigger_sos`, an unknown trip id is rejected, the assigned driver succeeds and the alert lands with `severity='critical'` and the correct `triggered_by`, the school's admin and the trip's one guardian each receive exactly one `sos` notification (the guardian's correctly tied to their child via `related_student_id`), and a post-`end_trip` call is rejected once the trip is no longer `in_progress`.

## Automated tests

Before this, every feature in this project was verified by hand — write a disposable-school script, hit the real Supabase REST API with real JWTs, eyeball the output, delete the data, move on. That caught real bugs but left zero regression protection. **Vitest** is the test runner (native ESM/TS, minimal config, nothing else in the repo pulls in a different one). The suite is deliberately split into two tiers with very different trust levels, because there is no separate test/staging Supabase project — only the real production one (`ubslfmtqebuuxujohksd`) — and the Supabase MCP tools to provision a second project aren't available in this environment.

- **Tier 1 — `packages/gps-sim/src/*.test.ts`** (`pnpm test`): pure-function unit tests for `engine.ts`/`route-utils.ts`/`telemetry.ts` — no network, no DB, fully safe to run on every push. `simulateInstantSpeedKmh`'s tests mock `Math.random` to deterministically hit each branch (jitter-only / speed spike / harsh brake) rather than asserting on random output.
- **Tier 2 — `supabase/tests/*.test.ts`** (`pnpm test:db`, **manual only, not run in CI**): formalizes the disposable-school pattern used all session into a committed, repeatable suite against the real database. `supabase/tests/helpers.ts` centralizes the fixture helpers (`createSchool`, `createUser`, `signIn`, `asUser`, `createBus`/`createRoute`/`createStudent`/`linkGuardian`, `deleteSchool`/`deleteUser`) — **credentials are read from env vars only, never hardcoded**, unlike the scratchpad scripts this was ported from, since this file is committed. Reads `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` from the root `.env.local` (same `dotenv` + path pattern as `scripts/seed.ts`). Cleanup is always school-before-users (school deletion cascades everything else; user deletion first hits the same `announcements.created_by` FK ordering issue the full-database-wipe session found).
  - `trigger_sos.test.ts` — a direct port of the SOS feature's original ad hoc verification script.
  - `check_in.test.ts` — QR check-in success path (attendance flips, guardian notified) plus the wrong-driver/unknown-token/trip-not-in-progress rejections.
  - `rls_school_isolation.test.ts` — formalizes the multi-school-hardening check this project already claimed was "verified live" once: a second school's admin gets zero rows back even when explicitly querying by the *other* school's id (students/buses/routes/the `schools` row itself).
  - `grant_lockdown.test.ts` — the one file needing a **direct Postgres connection** (`SUPABASE_DB_URL`, also root-`.env.local`-only, never committed): asserts `has_function_privilege` directly for `anon`/`public`/`authenticated` on a representative sample of RPCs. This can't be checked by just calling the RPC as an anonymous REST caller and asserting rejection — even with a leaked PUBLIC grant, the function's own business-logic check (`auth.uid()` is null) would *also* reject the call for an unrelated reason, giving false confidence. Direct privilege introspection is the only way to test the grant itself, same as the verification query this file's own docstring is drawn from (see "Grant-lockdown gotcha" above). Skips itself (`describe.skip`) rather than failing if `SUPABASE_DB_URL` isn't set.
  - `reports_fetchers.test.ts` — the one file that imports `@sabidrive/supabase` directly (`signInClient()` in `helpers.ts` returns a real signed-in client) and calls the actual `adminQueries.get*InRange` functions rather than re-implementing their REST calls by hand, so a bug in the real embed/select string would actually be caught. Needed `"jsx": "react-jsx"` added to `supabase/tests/tsconfig.json` — importing the package's barrel `index.ts` pulls its React hook files into the compile graph even though this suite never renders anything.
- `.github/workflows/ci.yml` — runs `lint`/`typecheck`/`build`/`test` (Tier 1 only) on every push/PR to `main`, using placeholder `NEXT_PUBLIC_*` values for the build step (both apps are client-side-only per the auth model above, so nothing at build time actually calls out to Supabase). **Deliberately does not run `pnpm test:db`** — that would mean production secrets in CI. Revisit full CI automation only if a dedicated test Supabase project gets provisioned later.
- `turbo.json` gained a `test` task; `supabase/tests`' own script is named `test:db` (not `test`) specifically so turbo's `test` pipeline never picks it up automatically alongside `gps-sim`. `pnpm-workspace.yaml` gained `"supabase/tests"` as a workspace package (previously only `apps/*`/`packages/*` were globbed).

## Admin reporting

Admin → Reports (`apps/admin/src/app/reports/page.tsx`) — everything the app tracked before this only existed as individual live records (Alerts lists raw alerts, SMS log lists raw texts); this is the first rollup view. A 7d/30d/90d range toggle (same `Button` pill-row pattern as the Alerts page's severity filter) drives four sections: Trips (total/completed/cancelled, average duration, by-route breakdown), Attendance (boarded/missed/excused/pending counts and rates), Incidents (alert counts by type/severity, resolution rate, top drivers by incident count), and SMS volume.

- **No new migration or RLS policy was needed** — `trips`/`attendance_expectations`/`alerts`/`sms_outbox` were already fully readable by an admin for their own school under existing policies; this is purely additive reads.
- **Fetch/compute split**, mirroring `packages/gps-sim`'s pure-math-vs-I/O separation: `packages/supabase/src/queries/admin.ts` gained four range-scoped fetchers (`getTripsInRange`, `getAttendanceInRange`, `getAlertsInRange`, `getSmsCountInRange`) that return raw rows; `packages/supabase/src/reports.ts` (new) holds the actual aggregation as pure functions (`summarizeTrips`/`summarizeAttendance`/`summarizeAlerts`) with no Supabase client — fully unit-tested (`reports.test.ts`, 11 cases against synthetic fixtures) rather than only verifiable by eyeballing a live page. This is the same Tier-1 pure-function pattern the test suite established, now extended to `packages/supabase` (new `vitest.config.ts` + `test` script there).
- `getAttendanceInRange` embeds `trips!inner(school_id, trip_date)` since `attendance_expectations` has no `school_id` of its own — filtering happens on the embedded table. `getAlertsInRange` embeds `trips(driver_id, driver:driver_id(full_name))` for the per-driver breakdown. Both are single, unambiguous FK paths (unlike the `trips`↔`buses` two-path case `bf90478` had to disambiguate with an explicit `!trips_bus_id_fkey` hint), so no embed hint is needed here.
- Bars in the by-route/by-type/top-drivers breakdowns are plain proportional-width `div`s against the `brand`/`critical` tokens — no charting library added, consistent with the calm-palette/no-heavy-dataviz style already established elsewhere.
- **CSV export**: an "Export CSV" button next to the range toggle downloads one file covering all four sections for the currently selected range. `packages/supabase/src/reports.ts` gained `buildReportsCsv()` (plus an internal `csvEscape` helper) — a pure function taking the same summary objects already computed for the on-screen view, so it's the summary that's exported (matching what's on screen), not raw rows: the existing `*InRange` fetchers return deliberately thin rows (no trip_date, student/bus names) that aren't rich enough for a meaningful raw export, and broadening them was out of scope for this addition. No new dependency — a `Blob` + temporary `<a download>` element is standard client-side download, same as the rest of this project's "no heavy dependency unless it earns it" pattern. 3 new test cases in `reports.test.ts` (full-section output, comma-in-name escaping, all-empty-range).
- **On-time performance** (previously deferred here as "real timezone-handling complexity" — now built): a fifth Card compares `stops.scheduled_time` (a bare, no-date `time`) against the actual arrival proxy — the earliest `check_in_events.occurred_at` for that `(trip, stop)` pair, matched by direction (`pickup` trips look for `board` events, `dropoff` trips look for `alight` events, same pairing the pickup/dropoff SMS-code feature already established). `packages/supabase/src/reports.ts` gained `zonedTimeToUtc(date, time, ianaTimezone)` — this project's **first** real use of `schools.timezone` for actual date math (previously stored/displayed only, never parsed anywhere). No timezone library: the standard "format a candidate instant in the target zone via `Intl.DateTimeFormat`, measure the offset, adjust" technique, using only the built-in `Intl` API — same minimal-dependency instinct that already kept a charting library out of this page. **Accepted simplification**: single-pass, not iterated to convergence across a DST transition boundary — wrong only in the specific hour of a DST shift, same tradeoff class as `packages/gps-sim/src/telemetry.ts`'s harsh-braking approximation. `summarizeOnTime()` groups by `(trip_id, stop_id)` (earliest check-in wins if a stop had multiple), classifies on-time/late/early against a 5-minute threshold (a hardcoded constant, not admin-configurable yet — same precedent as the speeding/harsh-brake thresholds), and tracks `skippedNoSchedule` separately so a stop missing a `scheduled_time` is honestly excluded rather than silently skewing the percentage.
  - **Honest limit, stated in the UI**: on-time performance can only be measured for a `(trip, stop)` pair that actually has a check-in event there — a stop nobody boarded/alighted at that day has no actual-arrival signal at all, so the empty state explains this rather than showing a bare "no data."
  - New fetcher `getOnTimeCheckInsInRange` follows the same `*InRange` shape as the other three, reusing the **already-existing** `adminQueries.getSchool()` (the Settings page's fetcher) for the school's timezone rather than adding a new query for it. No new migration/RLS — same "purely additive reads" precedent as the rest of this page.
  - 12 new Tier-1 test cases (`reports.test.ts`): the timezone conversion at a known non-DST and DST date, grouping/earliest-wins, direction/event-type matching, the exact-threshold boundary, and the null-`scheduled_time` exclusion. Plus a new Tier-2 live test in `reports_fetchers.test.ts`, needing two new `supabase/tests/helpers.ts` fixtures (`createStop`, `createCheckInEventDirect`) mirroring the existing `createTripDirect`/`createAttendanceDirect` service-role-insert pattern.

## In-app trip messaging

Every existing communication channel is one-way (notifications push out; nobody can reply). `supabase/migrations/0034_trip_messages.sql` adds a **per-trip shared channel**, not 1:1 direct messages — "running 5 min late" is relevant to every parent on that route, not just one, and scoping to a trip means reusing `can_view_trip()` (`0031_attendant_backup_crew.sql`) directly for both `SELECT` and `INSERT` RLS on the new `trip_messages` table, so this needed **zero new authorization logic**.

- `trip_messages` (`trip_id`, `sender_id`, `sender_name`, `body` — `check (char_length(body) between 1 and 1000)`, `created_at`) is a **plain RLS-governed client insert**, not an RPC — unlike `check_in`/`trigger_sos` there's no business logic beyond row-level scoping (can't impersonate another sender, can't post into a trip you can't view).
- **`sender_name` is denormalized onto the row by a `before insert` trigger** (`set_trip_message_sender_name()`, looks up `profiles.full_name` server-side and overwrites whatever the client sent) rather than read via a `sender:sender_id(full_name)` embed at query time. Caught live: no RLS policy lets one guardian read another guardian's `profiles` row — only driver↔guardian-of-their-route visibility exists (`0003`/`0015`/`0024`) — so an embed would've silently resolved to `null` whenever the sender was a *different* parent than the reader. Baking the name in at write time avoids adding new profiles RLS surface just to resolve a display string, the same way `notifications.body` already bakes `"New message from X"` in rather than making the reader separately resolve X.
- `notify_trip_message()` — an `after insert` trigger mirroring `queue_sms_fallback`'s existing trigger-does-the-fan-out shape (`0011_push_and_sms.sql`) rather than wrapping the primary insert in an RPC: inserts a `notifications` row (new `type = 'message'`) for every guardian on the trip's roster and the bus's driver/attendant, excluding the sender. This rides the existing `dispatch_push_notification` trigger for free (`0016_push_dispatch_trigger.sql`, fires unconditionally on any `notifications` insert, confirmed no type filter) — real push for messages with no new plumbing. **Deliberately not added to `queue_sms_fallback`'s type list** — real SMS per chat message would be spammy/costly; SMS fallback stays reserved for boarding/alighting/sos/mismatch/announcement.
- Both trigger functions (`returns trigger`) were confirmed live to reject direct invocation (`select public.notify_trip_message()` → `"trigger functions can only be called as triggers"`) — Postgres's own built-in safety, not something locked down manually, same as `queue_sms_fallback`.
- `packages/supabase/src/hooks/useTripMessages.ts` — new realtime hook, modeled directly on `useTripLocation.ts`'s shape (initial fetch + `postgres_changes` `INSERT` subscription filtered by `trip_id`). Returns `{ messages, sendMessage }`.
- Three call sites, no shared markup component (only three, each wants slightly different framing — not enough repetition to justify one, per this project's own "three similar lines beats a premature abstraction" convention): the driver's trip page (`apps/family/src/app/driver/trip/[tripId]/page.tsx`, below the SOS section), the parent's per-child page (`apps/family/src/app/parent/[studentId]/page.tsx`, shown only while a trip is active), and the admin trip detail page (`apps/admin/src/app/attendance/[tripId]/page.tsx`, so an admin can monitor or step in).
- Verified live end-to-end (disposable synthetic school, `supabase/tests/trip_messages.test.ts`, 7 tests): a driver not on the trip's bus and a guardian not on its roster are both rejected; empty and >1000-char bodies are rejected; a driver posting notifies both guardians but not themselves, with `sender_name` confirmed server-set even when the client tried to send a spoofed one; a guardian posting notifies the driver and the other guardian but not themselves; every participant can read the full thread.

## Offline GPS queuing

`useLiveLocationSharing.ts` (`apps/family/src/lib/useLiveLocationSharing.ts`) used to just set `status: "error"` and drop a GPS fix on any failure — a driver losing signal briefly (tunnel, dead zone) silently lost every location ping with no retry. This is the last item from the original post-launch feature list; the "phone goes dark" mitigations (`0031_attendant_backup_crew.sql`, `useStaleness`, manual admin override) all cover *device* failure, not a brief *network* gap on an otherwise-fine phone.

- On each fix, the hook now **flushes any already-queued points first** (oldest → newest, stopping the loop at the first failure so order is preserved), then attempts the current fix. A failure is only queued when `navigator.onLine === false` at that moment — a failure while online is a real rejection (e.g. trip no longer `in_progress`) and keeps surfacing as `"error"` exactly as before, never queued/retried forever.
- The queue persists to `localStorage` (`sabidrive:pending-locations:${tripId}`) so a backgrounded/killed-and-relaunched app doesn't lose it, capped at 100 entries (~7 min offline at the existing 4s cadence) — over the cap, oldest entries are evicted, since recency matters more than completeness for live tracking. A `window` `online` event also triggers a flush, as a safety net for when `watchPosition` itself has gone quiet.
- New `"queued"` status on the existing `LiveLocationStatus` union, with an honest, non-alarming label ("Offline — N location(s) queued") — same "tell the truth about connectivity" philosophy as `useStaleness.ts`. The driver's trip page shows this via the existing `StatusPill` plus a new Banner explaining queued positions send automatically and scanning/ending the trip work regardless.
- **The bounded-queue add/evict logic is pure and unit-tested** (extends the Tier-1 pattern): `packages/gps-sim/src/location-queue.ts`'s `enqueueLocationFix()`, tested in `location-queue.test.ts` — belongs alongside `telemetry.ts` since both deal with the same trip-location payload shape. The `localStorage`/`navigator.onLine`/React wiring stays in the hook itself, unlike the pure function, consistent with how this app's other browser-API-heavy hooks aren't unit tested.
- `supabase/migrations/0035_record_trip_location_timestamp.sql` — `record_trip_location` gains an optional trailing `p_recorded_at timestamptz default null`; the `trip_locations` insert uses `coalesce(p_recorded_at, now())`, so a replayed queued fix lands with its true original moment instead of replay time (which would otherwise corrupt the trip's actual position history). Hit the documented `CREATE OR REPLACE` overload trap again on purpose this time — i.e. accounted for it up front rather than rediscovering it — with an explicit `drop function` on the old 7-arg signature before recreating, then the usual grant-lockdown re-verification (`has_function_privilege`, not the advisor cache).
- **Explicitly out of scope**: the 60-second `speeding`/`harsh_brake` alert-cooldown dedup still compares against `alerts.created_at` (replay wall-clock time), not the original fix's elapsed time — a rare edge case (needs *multiple* alert-worthy events inside one offline window) and a deeper rework than this feature's core ask justifies. An accepted approximation, not a silent gap, same scoping class as the reports page's on-time-performance deferral.
- **Honest limit on verification**: true end-to-end offline-then-reconnect browser behavior can't be automated in this environment (no browser tool, and "offline" isn't a REST condition a Node script can simulate the way every other feature's live test could). Verified instead: 38 Tier-1 unit tests (queue logic, existing gps-sim math unaffected), a live RPC-level test (`supabase/tests/record_trip_location_timestamp.test.ts`) confirming the timestamp backfill/default-to-now behavior, and a dev-server smoke test of the driver trip page's new status/banner rendering.

## Native push notifications (Android, FCM — manual step required)

Web Push (`apps/family/public/push-worker.js`) is unreliable inside the Capacitor native shell — the "Native packaging" section above already flagged this as a known gap. This is the real fix, **Android only this round**: iOS needs a real Apple Developer account + APNs key and isn't buildable in this environment at all (no Mac), so speculative iOS wiring was explicitly left out rather than added un-verifiable.

- **Architecture**: `apps/family-native` ships no web assets of its own — `capacitor.config.ts`'s `server.url` points the WebView straight at the deployed `apps/family` site, so the JS that calls `@capacitor/push-notifications` has to be **`apps/family`'s own code**, not `family-native`'s. `@capacitor/core`/`@capacitor/push-notifications` are now dependencies of `apps/family` itself, gated behind `Capacitor.isNativePlatform()` in `apps/family/src/lib/nativePush.ts` — inert no-ops in a normal browser tab, where `NotificationOptIn.tsx` still uses the existing `subscribeToPush` (`apps/family/src/lib/push.ts`) unchanged.
- `supabase/migrations/0036_native_push_tokens.sql` — `native_push_tokens` mirrors `push_subscriptions_own`'s exact RLS shape (own-row `for all`), no new authorization pattern. `platform` intentionally only allows `'android'` right now — truthful to what's wired, extended later if/when iOS lands, same pattern as every other type-list extension in this project.
- **The Android Gradle guard already existed** — didn't need to write it. Capacitor's own default template (`apps/family-native/android/app/build.gradle`) already wraps `apply plugin: 'com.google.gms.google-services'` in a try/catch keyed on `google-services.json` existing, and the top-level `build.gradle` already had the `com.google.gms:google-services` classpath declared. All that was actually needed: `npx cap sync android` to register the plugin's own Gradle module (`capacitor.settings.gradle`/`capacitor.build.gradle`), and adding `POST_NOTIFICATIONS` to `AndroidManifest.xml` (required for `PushNotifications.requestPermissions()` to actually prompt on Android 13+/API 33, same as the existing `CAMERA` permission).
- **Verified live, not just written**: ran `gradlew assembleDebug` before touching anything (green baseline), again after `cap sync` registered the plugin module (green — the plugin's own Firebase Messaging dependency compiles fine without `google-services.json`, since the JSON is only needed by the *plugin*, not the *library classes*), and again after the manifest change (green) — confirming the currently-working Android build genuinely isn't regressed by any of this.
- `supabase/functions/push-dispatch/index.ts` — extended (not deployed, see below) to also look up `native_push_tokens` for the recipient and send via FCM HTTP v1, using `npm:google-auth-library@9` for the service-account → OAuth2 exchange (same `npm:` edge-runtime import pattern already proven by the existing `npm:web-push@3` import), then a plain `fetch` to `fcm.googleapis.com/v1/projects/{project_id}/messages:send`. **Gracefully no-ops the whole FCM branch** if `FCM_SERVICE_ACCOUNT_JSON` isn't set — identical defensive shape to `dispatch_push_notification`'s own Vault-secret check, so Web Push subscribers keep working regardless of whether FCM is configured.

**Status**: `push-dispatch` (version 8) is deployed and live at `https://ubslfmtqebuuxujohksd.supabase.co/functions/v1/push-dispatch` — deployed by the user directly via the Supabase CLI (`supabase functions deploy push-dispatch --project-ref ubslfmtqebuuxujohksd`), since neither the Supabase MCP tools nor the CLI were available in the environment that wrote the code. Confirmed live by calling it with no shared-secret header and getting back the function's own `401 unauthorized` (not a 404), proving the deployed code is genuinely running, not just committed.

**Manual steps still required** (same "code ships complete, external account setup doesn't" shape as Web Push/Twilio — the deploy step above is now done, these aren't):
1. Create a Firebase project, add an Android app with package name `com.sabidrive.family`, download `google-services.json` into `apps/family-native/android/app/` — this alone activates the already-guarded Gradle plugin, no code change needed.
2. Create a Firebase service account with the "Firebase Cloud Messaging API" role, download its JSON key, set it as the `FCM_SERVICE_ACCOUNT_JSON` Edge Function secret (Project Settings → Edge Functions → `push-dispatch` → Secrets, same place the VAPID keys live). The already-deployed function reads this at cold-start, so setting it doesn't need a redeploy.
3. Rebuild the Android app (`gradlew assembleDebug`) after step 1 so the new `google-services.json` gets picked up.

## Rate limiting on public endpoints

`apps/admin/src/app/api/signup-school` and `apps/family/src/app/api/login-with-phone` are the only two Route Handlers reachable with **no bearer token at all** — everything else already requires a verified session first. Neither had any throttling before this: `signup-school` could be hit to spam-create schools/admin accounts, and `login-with-phone` is a password-guessing surface — worse than a normal login form, since it calls `signInWithPassword` from *our own server*, so a distributed attacker's real IPs never reach Supabase Auth's own per-IP protections; every guess looks like it's coming from us.

- **Postgres-backed, not a new external service.** Vercel serverless functions don't share memory between invocations (an in-process `Map` wouldn't actually limit anything in production), and a Redis/Upstash-based limiter would mean yet another manual external-account setup on top of Twilio/Firebase/VAPID. `supabase/migrations/0037_rate_limiting.sql` adds `rate_limit_hits` (RLS enabled, **zero policies** — same "service-role only, never touched by a client session" pattern as `pickup_codes`) and `check_rate_limit(p_bucket, p_max_attempts, p_window_seconds)`, a fixed-window-log function: logs every attempt unconditionally (successful or not — an attempt is an attempt), then reports whether the bucket is still at or under the max within the window. **Revoked from `public`/`anon`/`authenticated` entirely** — tighter than the usual "revoke from anon, grant to authenticated" pattern, since this is never meant to be called from a browser session at all, only a Route Handler's service-role client (which bypasses grants by design and needs nothing explicitly granted).
- `purge_old_data()` gained one more line (`rate_limit_hits` older than 1 day) — same no-signature-change, safe-to-reissue edit every prior extension of that function has been.
- `packages/supabase/src/server.ts` gained `checkRateLimit()` and `getClientIp()` (reads `x-forwarded-for`, which Vercel sets — resolves to the real loopback address in local dev, not a placeholder string, learned the hard way when a first pass at writing the live-verification script assumed the wrong bucket key). **`checkRateLimit` fails open** (returns allowed) if the RPC call errors — a rate limiter is defense-in-depth, not a hard requirement, and failing closed would turn a transient DB hiccup into a full signup/login outage.
- `signup-school`: one bucket, `signup-school:{ip}`, 5/hour.
- `login-with-phone`: two buckets checked together, `login-with-phone:ip:{ip}` (10/15min, stops one IP spraying many phone numbers) and `login-with-phone:phone:{phone}` (5/15min, stops a distributed attempt against one target phone) — both must pass, and rejection never reveals which one tripped, same generic-error philosophy the route already had for "wrong phone" vs. "wrong password".
- Verified live end-to-end against the real local dev servers (not just the RPC in isolation): `/api/signup-school` hit 6 times in a row — 5 real disposable schools created and cleaned up, the 6th correctly `429`; `/api/login-with-phone` hit 6 times with the same bogus phone — the phone bucket correctly rejects on the 6th regardless of the IP bucket's separate, higher threshold. Plus 3 Tier-2 RPC-level tests (`supabase/tests/rate_limiting.test.ts`) covering allow-under-max, reject-at-max, and reset-after-window-passes.

## Notification preferences

A parent can mute specific notification types (Account page → new "Notifications" card) so they stop getting pushed/texted for them — `sos` is never mutable, it always delivers regardless, matching this project's existing critical/red-reserved-for-true-emergencies convention. Muting only suppresses **delivery** (push + SMS); the `notifications` row is always inserted either way, so a parent's in-app history stays complete even for a muted type — muting means "don't interrupt me," not "hide this from me if I go look."

- `supabase/migrations/0038_notification_preferences.sql` — `profiles.notification_prefs jsonb not null default '{}'::jsonb`. An absent key means "enabled" (the default), so every existing row needed zero backfill and any *future* new notification type automatically defaults to enabled for everyone without a migration. No new RLS policy needed: `profiles_update_own` already permits a client to write any column on their own row, including this one.
- Both `dispatch_push_notification()` and `queue_sms_fallback()` (`0016_push_dispatch_trigger.sql`/`0011_push_and_sms.sql`) gained a mute check — a `select coalesce((notification_prefs ->> new.type)::boolean, true) ...` lookup, skipped entirely (bypassed) when `new.type = 'sos'`. Both are plain `returns trigger` functions with no argument list, so `create or replace function` was safe to use directly — no `CREATE OR REPLACE` overload trap (that only bites when the *argument list* changes) and no grant-lockdown re-verification needed (trigger functions aren't callable via PostgREST in the first place).
- `packages/supabase/src/queries/users.ts` gained `updateNotificationPrefs()` — a plain direct `supabase.from("profiles").update(...)` call, no RPC, mirroring `push.ts`'s `upsertPushSubscription` as the precedent for "write my own settings row" (both backed by an equivalent own-row RLS policy). The Account page toggles save immediately per-click (optimistic local update + background persist, reverted on failure) rather than needing an explicit Save button.
- No `Switch`/`Toggle` primitive exists in `packages/ui` — the per-type toggle reuses the existing `Button` component as a two-state pill (same visual language as the Reports page's range toggle) rather than building a new styled-checkbox component for 7 rows.
- Verified live (`supabase/tests/notification_preferences.test.ts`, disposable guardian, no school needed since `notifications` only requires `recipient_id`/`type`/`title`): a muted type queues zero `sms_outbox` rows, an unmuted type queues exactly one, an `sos`-type notification still queues SMS even when the guardian's prefs explicitly set `sos: false`, and a signed-in guardian can write their own `notification_prefs` directly via the anon-key client. Push delivery itself (`dispatch_push_notification`'s `pg_net.http_post` call) isn't directly assertable in this suite — same limitation the original Web Push feature had — so verification leans on the SMS-fallback trigger, which *is* directly observable via `sms_outbox`, as the proxy for "the mute check in both trigger functions fires correctly."

## Bulk CSV import for students

Admin → Students gained an "Import CSV" card (alongside the existing one-at-a-time "New student" form) to onboard a school's full roster in one step. **Scoped to students only** — stops/routes were deliberately left out after research surfaced a real problem: every stop's `lat`/`lng` today comes from either a map click or geocoding an address through OpenStreetMap's Nominatim API (`AddressSearch.tsx`), a single-request-per-submit client-side call with no rate-limiting, queueing, or server-side proxy, already relying on Nominatim's own usage policy against automated/bulk querying (see "Address search for routes and stops" above). Batch-geocoding many CSV rows would risk violating that policy or getting the app's IP blocked, with no existing safeguard to make it safe — a genuinely different, harder problem than importing students, which need no geocoding at all.

- **This is the first file-upload UI in the app** — no `FileReader`/`.csv`-parsing/`input type="file"` code existed anywhere before this (the Reports page's CSV work is an *export*, the opposite direction). No `papaparse` dependency added: `packages/supabase/src/studentImport.ts` gained a small hand-rolled `parseCsv()` state-machine parser (quoted fields with embedded commas/quotes/newlines, doubled-`""` escaping, CRLF/LF) — a naive `.split(",")` breaks on any quoted field, common enough in real school data to get wrong silently, matching the same "no new dependency unless it earns it" reasoning the CSV *export* feature already established.
- `buildStudentImportPlan()` (same file, pure, no Supabase client) resolves a CSV's `route`/`stop` *names* against the school's already-loaded route/stop lists (the Students page already fetches `{id, name}`/`{id, name, route_id}` for its existing dropdowns — zero new queries) case-insensitively, reporting `"route not found"`/`"ambiguous route name"` (route `name` has no uniqueness constraint in the schema, so duplicates are a real possible case) per row rather than failing the whole file. **Partial success by design**, same precedent as the CSV export feature favoring a useful partial result: rows that fail validation are listed with their row number and reason but don't block the valid rows from importing.
- `packages/supabase/src/queries/admin.ts` gained `createStudentsBulk()` — a single multi-row `.insert([...]).select()`. No new migration/RLS: `students_admin_crud` is a plain row-level policy with no batch-size restriction, so this already worked under existing RLS — `createStudent` was single-row purely as an application-code choice matching the single-row form, not a policy limitation.
- A "Download template" button (same client-`Blob`-download pattern as the Reports CSV export) gives the exact expected header row + one example line, so an admin doesn't have to guess the format.
- 17 new Tier-1 tests (`studentImport.test.ts`: parser edge cases + every validation-error path) plus 2 Tier-2 live tests (`supabase/tests/student_import.test.ts`): a multi-row bulk insert correctly resolves route/stop assignment per row, and a cross-school `school_id` in the payload is rejected by RLS.

## Parent trip history

The per-student page (`apps/family/src/app/parent/[studentId]/page.tsx`) was strictly live-trip-only — no way to see past trips, only ever querying `trips` for `status = 'in_progress'`. A new "Trip History" link opens `apps/family/src/app/parent/[studentId]/history/page.tsx` (a separate page, same precedent as `/parent/announcements` — the live trip page is already dense; this is a distinct "look at the past" mode, not another section bolted onto it), listing the student's last 30 days of trips.

- **No new migration was needed for trip/attendance visibility** — `can_view_trip()` (gating `trips`/`check_in_events`/`attendance_expectations`) has no `status = 'in_progress'` filter at all; visibility is keyed on an `attendance_expectations` row existing for the guardian's child, created once at `start_trip()` and never deleted. A parent could already read every past trip for their child; the only real gap was that no family-app query did date-range fetching (the `*InRange` pattern existed only in `packages/supabase/src/queries/admin.ts`).
- **A real RLS gap was found live, not assumed from reading the policy**: `stops_select_parent` only covered a student's *current* `default_stop_id`, so a guardian couldn't see the stop name for a past check-in at any other stop (a real case after a route/stop reassignment — and exactly what the new Tier-2 test hit immediately, since its test student had no default stop assigned at all). `0039_trip_history_stop_visibility.sql` extends the policy with a second clause granting visibility for any stop the guardian's child has an actual `check_in_events` row at — the same "historical access via a permanent linking row" pattern `can_view_trip`/`attendance_expectations` already established, purely additive (verified the existing RLS isolation and check-in test suites still pass unchanged).
- `queries/students.ts` gained `getTripHistoryForStudent()`, anchored on `attendance_expectations` (not only `check_in_events`) so a missed pickup still shows up rather than being silently absent — a trip with no matching check-in renders with null checked-in-time/on-time fields instead of not appearing at all.
- `packages/supabase/src/tripHistory.ts` (new, pure, no Supabase client) reuses `reports.ts`'s `zonedTimeToUtc()` as-is for the same on-time/late/early deviation math the admin Reports page uses, but reshaped as a **per-trip list for one student** rather than a school-wide `byRoute` aggregate — `summarizeOnTime()` itself wasn't reusable unmodified, its output shape is a single collapsed summary object, not a list.
- `StatusPill`'s `statusToneMap` (`packages/ui`) gained `on_time`/`late`/`early` entries, reusing the existing tone system rather than inventing new styling.
- 7 new Tier-1 tests (`tripHistory.test.ts`) plus 2 Tier-2 live tests (`supabase/tests/trip_history.test.ts`): a boarded trip and a missed trip both resolve correctly for the linked guardian, and a guardian with no link to the student is rejected outright (they can't even read the student row, so the whole call throws rather than silently returning nothing).

## Driver route manifest

The driver home page (`apps/family/src/app/driver/page.tsx`) showed only the assigned bus/route name and a Start/Resume Trip button — no stop list, no student roster, so a driver had no picture of the day's route until stops showed up one at a time on the Scan screen. A new "Today's Route" card, shown before the Start Trip button, lists every stop in order with its scheduled time and the students assigned there.

- **No new migration/RLS needed** — `routes_select_driver`/`stops_select_driver`/`students_select_driver` (`0003_rls_policies.sql`, untouched since) are all scoped purely by the driver's assigned bus's `default_route_id`, with zero dependency on a trip existing. A driver could already read every stop and student on their route pre-trip; the manifest itself (`students.default_route_id`/`default_stop_id`, static assignment data) needed no `attendance_expectations` row at all — unlike the trip page's attendance list, which only exists after `start_trip()` runs.
- **This grouping logic didn't exist anywhere** — grepped every use of `default_stop_id`; the admin route-detail page lists stops in order but never joins students onto them. `packages/supabase/src/routeManifest.ts` (new, pure, no Supabase client) is genuinely new: `buildRouteManifest()` groups students by `default_stop_id` into stop order, and honestly surfaces students with no stop assigned (or a stale `default_stop_id` that no longer matches any stop on the route) in a separate `unassignedStudents` bucket rather than silently dropping them — a real data-quality case, not paranoia.
- `queries/trips.ts` gained `getRouteManifest()` alongside `getDriverBus`, same fetch/compute split as the rest of this session's features (raw rows in, pure grouping separate).
- 5 new Tier-1 tests (`routeManifest.test.ts`) plus 2 Tier-2 live tests (`supabase/tests/route_manifest.test.ts`): correct stop/student grouping for the assigned driver, and a driver not assigned to that route gets nothing back — the RLS boundary, exercised live.

## CSV export for Alerts and SMS log

Extends the Reports page's CSV export pattern to the Alerts and SMS log pages, which show raw per-record lists with no export at all before this — an "Export CSV" button next to each page's existing content, same `Blob` + temporary `<a download>` pattern, no new dependency.

- **`csvEscape`/`csvRow` moved out of `reports.ts` into a new shared `packages/supabase/src/csv.ts`** — this is the third call site needing them (Reports' own export was the first), crossing this project's own "three similar things" threshold. `reports.ts` now imports from `csv.ts` instead of defining private copies; behavior-preserving, confirmed by the existing 25-case `reports.test.ts` suite passing unchanged after the extraction.
- `packages/supabase/src/adminCsvExports.ts` (new) holds `buildAlertsCsv()`/`buildSmsCsv()` — flat one-row-per-record exports, unlike `buildReportsCsv`'s grouped rollup shape, so they didn't belong in `reports.ts` either (that file's own header comment scopes it to the Reports page specifically).
- **Honest, stated scope limit**: both `getSchoolAlerts`/`getSmsOutbox` are hard-capped (50/100 rows) with no pagination or date range today, so export is "the same rows currently on screen," not a full history — a caption next to each button says so explicitly rather than implying completeness. Adding pagination/date-range to either page would be a separate, bigger feature.
- Alerts export respects the page's existing severity filter (exports `filteredAlerts`, not the unfiltered set) — consistent with "export what you're looking at." `assigned_to` exports as `Yes`/blank rather than the raw uuid, matching the on-screen semantic (the page itself never resolves it to a name either, just an "assigned" badge).
- 7 new Tier-1 tests (`adminCsvExports.test.ts`): header/row correctness for both builders, comma-escaping in notes/body, `assigned_to` id-vs-blank, empty-input handling.
