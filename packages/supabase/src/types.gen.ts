/**
 * Placeholder — replace with the real output of the Supabase MCP
 * `generate_typescript_types` tool after migrations are applied.
 * Until then this keeps `Database` generic enough for the app to type-check.
 */
export type Database = Record<string, unknown>;
